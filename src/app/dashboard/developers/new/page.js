import { UserCreateView } from 'src/sections/developers/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Property',
};

export default function UserCreatePage() {
  return <UserCreateView />;
}

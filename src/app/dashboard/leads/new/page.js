import { UserCreateView } from 'src/sections/leads/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Lead',
};

export default function UserCreatePage() {
  return <UserCreateView />;
}
